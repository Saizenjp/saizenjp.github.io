{
  "name": "saizen-ops",
  "version": "1.0.0",
  "private": true,
  "description": "SaiZen 야마나미 리조트 운영 통합 시스템",
  "scripts": {
    "check": "node scripts/check-syntax.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "verify": "npm run check && npm test"
  }
}
