# 15. 개선 백로그 (IMPROVEMENT BACKLOG)

> **저장소명**: `saizenjp/saizenjp.github.io`
> **브랜치**: `main`
> **기준 커밋**: `8cd0ad8` (`8cd0ad8f8f68db0a2c9679e2c6452c769846b000`)
> **작성일**: 2026-07-21
> **문서 상태**: 기술기준 (코드·감사문서 근거)
> **분석 범위**: 개선 과제(긴급/단기/중기/장기)
> **제외 범위**: 실제 데이터

근거 문서: `docs/pre-deploy-audit.md`, `docs/maturity-kit.md`, `docs/future-work.md`, `13_SECURITY_AND_RISKS`, `14_KNOWN_ISSUES`.

---

## 🔴 긴급 (Critical)

### C1. 정산 뷰 RLS 우회 라이브 적용 확인
- **문제**: `v_folio_balance` 등 5개 정산 뷰가 `security_invoker` 없이 생성되면 권한 없는 staff가 전 팀 매출·잔액 열람.
- **영향**: 금전 PII 노출(RLS 우회).
- **개선안**: `ops/hub/sql/39_settle_views_security_invoker.sql`을 라이브 DB에 적용했는지 `00_VERIFY.sql`로 확인·재적용.
- **관련 파일**: `sql/09`·`39`·`00_VERIFY.sql` · **난이도**: 하 · **선행조건**: Supabase 접근.

### C2. 마이그레이션 라이브 적용 전수 확인
- **문제**: 수동 SQL 실행 + 적용 추적 부재로 RLS·트리거가 실제 DB에 반영됐는지 불확실(17~87).
- **영향**: 보안·정합성 정책이 코드에만 있고 DB엔 미적용일 위험.
- **개선안**: `00_VERIFY.sql`(현재 87까지 확장) 실행 → 전 항목 ✅ 확인. ❌ 항목 즉시 적용.
- **관련 파일**: `sql/00_VERIFY.sql` · **난이도**: 하 · **선행조건**: Supabase 접근.

### C3. 객실 더블부킹 DB 제약 검증
- **문제**: 정원/겹침을 클라이언트가 주로 검사. DB 트리거(`40`)는 있으나 EXCLUDE 제약은 없음 → 동시편집 더블부킹 여지.
- **영향**: 데이터 정합성(중복 배정).
- **개선안**: `rooms_capacity_guard` 트리거 라이브 적용 확인 + 필요 시 EXCLUDE 제약 검토.
- **관련 파일**: `sql/40`·`67`·`room.html` · **난이도**: 중 · **선행조건**: C2.

---

## 🟠 단기 (Short-term)

### S1. settle_merit 인원 과소청구 방지
- **문제**: 부분 import 시 명단수(PCNT)가 예약 pax보다 우선 → B2B 절반 청구 가능.
- **영향**: 매출 누락.
- **개선안**: `max(명단,예약)` 사용 + 불일치 경고 배지(이미 일부 반영 — 라이브 확인).
- **관련 파일**: `settle_merit.html` · **난이도**: 하.

### S2. `guests.check_status` 상태전이 구현/명확화
- **문제**: 임포트 시 `'체크인전'` 고정, 체크인/체크아웃 전이 갱신 코드 미발견.
- **영향**: 체크인 상태가 `key_bindings`·날짜로만 추정됨.
- **개선안**: 키택 바인딩/프론트데스크에서 `check_status` 갱신 로직 추가하거나, 미사용이면 컬럼 폐기 명문화.
- **관련 파일**: `keytag.html`·`frontdesk.html`·`step1.html:802` · **난이도**: 중 · **선행조건**: 운영 정책 확정.

### S3. jsdom 스모크 테스트 CI 편입
- **문제**: CI가 문법+코어 단위테스트만. 페이지 렌더·핵심 함수 통합 미검증.
- **영향**: 회귀 사각(렌더·집계).
- **개선안**: `docs/maturity-kit.md` 권고대로 jsdom 스모크 자산화 → CI 추가.
- **관련 파일**: `.github/workflows/ci.yml`·`tests/` · **난이도**: 중.

### S4. Dead/orphan 코드 정리
- **문제**: `mountUser`(dead), `/app/` `buildTransfer`/`renderGolf`(nav 미노출) 잔존.
- **영향**: 유지보수 혼란.
- **개선안**: 사용 여부 확정 후 제거 또는 명시적 격리 주석.
- **관련 파일**: `ops/assets/saizen-ops.js:1123`·`app/index.html:2239,3371` · **난이도**: 하.

---

## 🟡 중기 (Mid-term)

### M1. /app/ ↔ /ops/ 도메인 로직 완전 이관(SZCore)
- **문제**: 규칙이 양쪽에 이중 정의될 여지(태그·인쇄 계산 등).
- **영향**: 규칙 변경 시 이중 수정·불일치.
- **개선안**: `maturity-kit.md:113` 원칙대로 이중 정의 함수부터 `saizen-core.js`로 이관 + 테스트.
- **관련 파일**: `app/index.html`·`ops/assets/saizen-core.js` · **난이도**: 중 · **선행조건**: 대조 감사.

### M2. 스테이징 분리
- **문제**: `main` 푸시가 즉시 프로덕션(무중단 검증 계층 없음).
- **영향**: 실수 시 라이브 직접 영향.
- **개선안**: Supabase Branching(Pro) + 스테이징 URL 활용.
- **관련 파일**: 배포 파이프라인 · **난이도**: 중.

### M3. 마감(정산 잠금) 기능
- **문제**: 일/월 마감·folio 잠금 기능 미발견.
- **영향**: 마감 후 변경 통제 부재.
- **개선안**: 마감 상태·잠금 도입(운영 정책 확정 후).
- **관련 파일**: `settle.html`·`sql/09` · **난이도**: 중 · **선행조건**: 마감 기준 확정.

---

## 🟢 장기 (Long-term)

### L1. 송영 배차 시스템화
- **문제**: 송영 배차(운영)는 백로그(정산 요금만 존재).
- **개선안**: ops에 배차 화면 도입(차량·시간·인원 배정).
- **관련 파일**: (신규)·`settle_merit.html`(요금 연동) · **난이도**: 상.

### L2. 에러 모니터링·백업 복구 리허설
- **문제**: 전역 에러수집 없음, 백업 복구 미실연습.
- **개선안**: 클라이언트 에러 수집 + Supabase 백업 복구 절차 문서·리허설.
- **난이도**: 중.

### L3. 레스토랑 명패 QR 병기 등 기능 백로그
- **근거**: `docs/future-work.md`(POS 실운영 후 착수).
- **난이도**: 중.

---

## 우선순위 요약

| 순위 | 과제 | 근거 |
|---|---|---|
| 1 | C1·C2(정산 뷰·마이그레이션 라이브 확인) | 보안·정합성 직결 |
| 2 | C3·S1(더블부킹·과소청구) | 데이터·매출 |
| 3 | S2·S3·S4(상태전이·CI·정리) | 정확성·유지보수 |
| 4 | M1~M3, L1~L3 | 구조·기능 확장 |
