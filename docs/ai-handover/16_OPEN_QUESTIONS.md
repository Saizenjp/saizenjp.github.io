# 16. 오픈 퀘스천 (OPEN QUESTIONS)

> **저장소명**: `saizenjp/saizenjp.github.io`
> **브랜치**: `main`
> **기준 커밋**: `8cd0ad8` (`8cd0ad8f8f68db0a2c9679e2c6452c769846b000`)
> **작성일**: 2026-07-21
> **문서 상태**: 기술기준 (코드만으로 판단 불가한 항목)
> **분석 범위**: 코드 밖 확인이 필요한 질문 목록
> **제외 범위**: 실제 데이터

> 코드만으로 답할 수 없는 사항을 질문 형태로 정리한다. 각 질문에 관련 근거 파일을 병기한다.

---

## 1. 구현됐지만 실제 사용 여부가 불분명한 기능

- Q1. **`/app/index.html`(레거시 단일 HTML)** 은 현재도 병행 사용 중인가? ops로 완전 이전됐는가? (근거: `app/index.html`, `CLAUDE.md`가 "병행 사용"이라 기재하나 실사용 빈도 불명)
- Q2. **`translate-remarks` Edge Function** 은 배포·호출되고 있는가? 어떤 번역 엔진(DeepL 등)·키를 쓰는가? (근거: `ops/edge-functions/translate-remarks/index.ts`, `sql/64_remark_ja.sql`)
- Q3. **`design/` 폴더**(토큰·컴포넌트 미리보기)는 런타임에 쓰이나, 개발 참조용인가? (근거: `design/`)
- Q4. **골프 조편성(`golf.html`)·재고(`inventory.html`)·공지(`board.html`)·팀메모(`notes.html`)** 는 실제 운영에서 사용되는가? (근거: 해당 페이지)
- Q5. **`pos_customer.html`(손님 태블릿)·`bill.html`(QR 청구)** 는 현장 배치·운영되는가? (근거: `sql/62`·`73`)

## 2. 코드와 현장 운영이 다를 가능성

- Q6. **송영 배차**는 시스템 밖(수기 엑셀 등)에서 어떻게 처리되는가? (배차는 미구현, 정산 요금만 존재 — `08_MEAL_SHUTTLE_OPERATION`)
- Q7. **전(全)예약을 브라우징하는 "예약목록" 화면**이 없다. 현장은 frontdesk 리스트로 충분한가, 별도 목록이 필요한가? (근거: 조사 결과 전용 페이지 부재)
- Q8. 문서(`CLAUDE.md`)와 코드의 **마이그레이션 번호 차이**(문서 01~16 요약 vs 실제 87) — 운영 기준은 코드가 맞는가? (규칙상 "코드가 정답"이나 재확인)

## 3. 객실 배정 예외

- Q9. **소보별장·아소별장**을 자동배정에서 제외하는 현장 규칙의 근거·범위는? (근거: `room.html` autoRoute)
- Q10. **`싱글` 지정 팀·3명팀·홀수 잔여** 보류 후 수기 처리 절차는? (근거: `room.html:2459-2464`)
- Q11. **박수→층 청소효율 규칙**(3·4박→9·6·3층 등)은 현재도 유효한 현장 정책인가? (근거: `SZCore.allowedFloors`)
- Q12. **시즈노야도·간지호텔 전용 카드 분리**(메모리상 언급)를 진행할 것인가? (근거: `CLAUDE.md`, `shizu.html`)
- Q13. `rooms.member_id` 라이브 실제 타입(파일상 `bigint` vs `uuid` 불일치)? (근거: `sql/06`)

## 4. 식사 및 송영 예외

- Q14. **간지·시즈 요일 규칙**의 예외(특이 출·귀국)는 수기 보정하는데, 그 빈도·기준은? (근거: `SZCore.mealPlan`)
- Q15. **워크인(일본 고객)·테이블No** 식수는 `/app/` localStorage 수기 → ops로 이전할 것인가? (근거: `dinner.html`)
- Q16. **夕食 除外·別注 업그레이드**의 청구 연동(단가 있는 업그레이드만 charges)이 현장 회계와 일치하는가? (근거: `sql/58`)

## 5. POS 마감 기준

- Q17. **일/월 마감(정산 잠금)** 기준·주체·시점은? (코드상 명시적 마감 배치 미발견 — `09_POS_SETTLEMENT_FLOW` §6)
- Q18. folio `status`(open→closed→settled) 전이의 **운영 규칙**(누가 언제 closed/settled로)?
- Q19. **void(취소) 처리 권한·사유 기록** 정책은?
- Q20. **현장 정산(settle) vs B2B(settle_merit)** 를 실제 회계에서 어떻게 합산·대사하는가? (코드상 별개 레이어)

## 6. 수작업 처리 단계

- Q21. **F풀 미보유/불일치(F접두) 회원 코드 보정**(groupcodes)은 정기적으로 누가 수행하는가? (근거: `groupcodes.html`, `sql/81/87`)
- Q22. **키택 바인딩(체크인)** 은 항상 수기인가? 자동화 여지는? (근거: `keytag.html`)
- Q23. **Excel 업로드 주기·담당자**(월 단위 동기화 트리거)? (근거: `step1.html`)
- Q24. **SQL 마이그레이션 실행**은 누가·어떤 절차로 하는가(수동)? (근거: `11_DEPLOYMENT_GUIDE`)

## 7. 현재 운영 정책 확인이 필요한 부분

- Q25. **권한(area) 부여 정책** — 어떤 역할에 어떤 영역을 주는가? member_codes(PII) 접근 대상 최소화 기준? (근거: `admin.html`, `sql/19/46/65`)
- Q26. **개인정보 보유기간·파기** 정책(passengers 여권·생년 등)? (근거: `privacy.html`, PII 테이블)
- Q27. **백업·복구** 절차와 리허설 주기(Supabase Pro)? (근거: `14_KNOWN_ISSUES` §5)
- Q28. **초대 계정 발급·비활성(차단)** 운영(마스터 단독)? (근거: `sql/28/49`, `admin.html`)
- Q29. **스테이징 없이 main 직결 배포**를 유지할 것인가, 분리할 것인가? (근거: `docs/pre-deploy-audit.md`)

---

> 위 질문들은 코드 근거는 있으나 **운영 정책·현장 실태 확인**이 필요한 항목이다. 인수인계 시 담당자 인터뷰로 답을 채워 각 문서의 `확인 필요` 표시를 해소할 것.
