-- ============================================================================
--  80_team_group_seq.sql — 운영팀 묶음의 '순번'(그룹 내 정렬) 컬럼
-- ----------------------------------------------------------------------------
--  現地手配書에서 3팀 이상 묶을 때 대표자 설정 팝업에서 순번(Y1/Y2/Y3)을 수동
--  지정 → 手配書 화면 목록·一括印刷이 그 순서로 출력. NULL이면 기존 자동정렬
--  (SZCore.orderGroup: 대표 먼저→dep→seq)로 graceful fallback. 멱등. ⚠ 수동/MCP.
-- ============================================================================

alter table public.print_overrides add column if not exists team_group_seq int;
