-- ============================================================================
--  66_team_group_rep.sql — 운영팀 묶음의 '대표팀' 지정 컬럼
-- ----------------------------------------------------------------------------
--  現地手配書에서 팀을 묶을 때 대표팀을 지정 → 그 대표의 태그코드를 그룹
--  기준코드로 삼아 팀단위 인쇄물은 base+번호(DRな-Y1/Y2), 네임택은 연속
--  개인번호(DRな-1Y…)로 통일. 대표=team_group_rep=true(그룹당 1팀).
--  team_group(그룹 라벨)은 기존 그대로. 멱등. ⚠ 수동/MCP 적용.
-- ============================================================================

alter table public.print_overrides add column if not exists team_group_rep boolean not null default false;
