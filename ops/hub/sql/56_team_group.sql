-- ============================================================================
--  56_team_group.sql — 운영 팀(現地手配書의 チーム) 라벨
-- ----------------------------------------------------------------------------
--  목적: 여러 행사를 한 '운영 팀'으로 묶는 라벨. 現地手配書에서 정의·표시.
--    · print_overrides.team_group (text) = 운영 팀 라벨(예: '메리트 6월A조').
--      같은 라벨 = 같은 팀. 手配書 チーム 칸에 표시.
--    · 夕食 합석(dining_group)과는 별개 — 夕食은 이 값을 '뱃지'로 참고만(읽기).
--  RLS: print_overrides 기존 정책 그대로(읽기 로그인·쓰기 room/print 영역).
--  멱등. ⚠ Supabase SQL Editor 수동 실행(또는 MCP). 55 이후.
-- ============================================================================

alter table public.print_overrides add column if not exists team_group text;
