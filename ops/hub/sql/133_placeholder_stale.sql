-- ============================================================================
-- 133_placeholder_stale.sql — 데이터 검수 ⑭ 「이름 붙은 뒤 남은 무기명 줄」
--
--  엠클릭 명단은 처음엔 이름 없이(무기명) 오고, 나중에 이름·생년이 채워진다.
--  step1 의 사람 식별키가 무기명|#자리 → 이름|생년 으로 바뀌면서 새 줄이 생기고 옛 줄이 남았다.
--  유령 청소는 「절반 넘게 지우면 키 불일치」로 보류해 옛 줄이 방을 그대로 쥐고 있었다
--  (실측 2026-09: 7팀 25줄, 자동배정 방 25건 이중 점유).
--  · step1: upsert 전에 같은 자리의 무기명 줄 키를 새 키로 바꿔 **같은 줄을 이어받는다**(planPlaceholderRenames).
--  · data_audit: 남은 것을 계속 본다(placeholder_stale).
--  ⚠ 함수 전문은 Supabase 적용본이 정본 — ①~⑬ 은 129·132 와 같고 ⑭ 블록이 붙는다.
-- ============================================================================
revoke execute on function public.data_audit() from public;
grant  execute on function public.data_audit() to authenticated;
